<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.semester_6">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Semester_6">
        <activity android:name=".loginactivity"></activity>
        <activity android:name=".itemdetails_mysold" />
        <activity android:name=".itemDetails_cart" />
        <activity android:name=".myCart" />
        <activity android:name=".ui.home.itemDetails" />
        <activity android:name=".registrationpage" />
        <activity android:name=".sellPage" />
        <activity
            android:name=".nav"
            android:label="@string/title_activity_nav"
            android:theme="@style/Theme.Semester_6.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>